# Состояние проекта

Дата: 17.09.2026

## Исполнитель и реквизиты
Сайт оформлен на **Александру Мельникову** (самозанятая, ИНН 432902125410) — это указано в шапке,
подвале, политике конфиденциальности, микроразметке и OG-картинке. Контакты: Telegram `@melnikova_ad`,
почта `aleksa.ponomareva2001@mail.ru`. В текстах используется женский род («Проверила вживую»).
Данные вынесены в `src/data/site.ts` → `site` и `legal`.

## Публикации
| Проект | Адрес | Папка на хостинге |
| --- | --- | --- |
| Портфолио Александры Мельниковой | https://rootlost.online/ | `/www/rootlost.online` |
| Политика конфиденциальности | https://rootlost.online/privacy.html | там же |
| Демо-сайт «ТехРемонт» | https://rootlost.online/remont/ | `/www/rootlost.online/remont` |
| Сайт «КИТ-Строй.Москва» (тема WordPress) | https://rootlost.ru/ | `/www/rootlost.ru/wp-content/themes/kitstroy-moscow` |

SSL на `rootlost.online` работает: и `http`, и `https` отвечают 200.
Пароль FTP в файлах проекта не хранится — передаётся через переменные окружения.

## Портфолио: концепция «печатный каталог»
Светлая редакционная вёрстка: бумага `#F4F1EA`, чернила `#14120F`, киноварь `#D33A1C`, зерно бумаги,
антиква Playfair Display в заголовках, Inter в тексте, JetBrains Mono в метках. Работы — список-индекс
с превью, следующим за курсором. Жёсткие тени, прямые углы, штампы, бегущие строки, счётчики,
проявление заголовка при загрузке.

### Что изменилось по замечаниям
- **Превью кейса** больше не появляется в углу: следует за курсором с плавным догоном, прижимается
  к курсору, не заезжает под шапку и не выходит за края экрана. Проверяется автотестом.
- **Концепты убраны** с сайта: в портфолио только реальные проекты. Данные концептов сохранены
  в `src/data/concepts.ts` (не выводятся, не попадают в сборку) на случай возврата.
- **Кейс «Юлия и Никита» удалён.** Для проектов без живой ссылки ссылка просто не показывается,
  надписей «концепт» и «учебный проект» на сайте нет.
- **В карточке кейса появилась мини-форма связи**: поле «Telegram, телефон или e-mail», кнопка
  «Отправить» (копирует текст и открывает чат Telegram), ссылка на политику и запасной вариант по почте.
- **Числа контрольных ширин убраны** из всех текстов — вместо «360, 768, 1440» формулировки
  «телефон, планшет и большой экран». В коде остались только медиазапрос и атрибут картинки.
- **Работа по ТЗ и идее**: добавлена услуга «Сайт под ключ», обновлены первый экран, блок процесса
  («Бриф и материалы») и причина «По макету, ТЗ или идее».
- **Юридическая часть**: уведомление об использовании cookie с выбором и запоминанием, страница
  политики конфиденциальности (`privacy.html`, 8 разделов), согласие на обработку данных в квизе
  и в форме кейса. Аналитики и сторонних трекеров на сайте нет — так и написано в политике.

## Разбор визуальных дефектов (последняя правка)
Владелец заметил два дефекта: пустые полосы на странице кейса и пустой главный кейс в рамке.
Оба подтвердились, причины оказались разными.

### 1. Пустые полосы на `case.html` — не вызывался наблюдатель появления
Наблюдатель появления блоков (`useRevealOnScroll`) вызывался только в `App.tsx`. На странице
кейса его не вызывал никто, поэтому **17 секций с `data-reveal` оставались с `opacity: 0`**:
страница выглядела как набор пустых полос, текст появлялся только там, где анимации не было.

Исправление:
- `src/lib/reveal.tsx`: наблюдатель один на страницу и живёт вне React — `initReveal()`.
  Он же следит за новыми узлами через `MutationObserver`, поэтому поздно отрисованные
  блоки тоже появляются.
- `initReveal()` вызывается в `App.tsx`, `case.tsx` и `privacy.tsx`.
- Правило записано в `AGENTS.md`: точка входа новой страницы обязана вызвать `initReveal()`.
- Проверка: `node scripts/audit-visual.mjs` прокручивает страницу как человек и падает,
  если хоть один блок не появился.

### 2. Пустой главный кейс — снимок сделан до появления блоков
`scripts/case-shots.mjs` раскрывал только селекторы портфолио (`[data-reveal]`, `.is-in`),
а демо-сайт мастерской прячет секции классами `.head`, `.card`, `.promise`, `.step`, `.cta`.
В результате `forma-full.webp` снимался до анимации: **83 % высоты кадра были пустыми**
(7 полос по 370–600 px), и в рамке на странице кейса вместо сайта показывался чёрный лист.
Второй причиной было то, что `captureBeyondViewport` снимает страницу без прокрутки:
наблюдатели появления при этом не срабатывают вообще.

Исправление:
- общий модуль `scripts/lib/audit.mjs` с двумя выражениями: `SCROLL_THROUGH` (прокрутка
  по экрану с паузами) и `REVEAL_ALL` (принудительное раскрытие блоков обоих проектов);
- `case-shots.mjs` перед снимком прокручивает страницу целиком, раскрывает блоки, возвращает
  её наверх и только потом снимает;
- готовый кадр проверяется на однотонные полосы (`scripts/lib/image-bands.mjs`): если пусто
  больше половины высоты, снимок повторяется. Это защита от повторения ошибки.

### 3. Провалы между секциями на странице кейса
Замеры показали 207–273 px пустоты между секциями: каждая секция давала по 120 px отступа,
и на странице кейса, где секции идут подряд, это читалось как разрыв. Добавлены
`.section-tight` (88 px на большом экране) и `.section-join` (линейка на стыке однотипных секций);
применены к «Экранам», «Что из этого можно заказать», «Заявке» и «Другим работам».
Секция «Заявка» переведена на бумажный фон — она больше не смотрится врезкой.
Было 4 провала больше 200 px, стало 0.

### 4. Кодировка: три потерянных символа
`check-mojibake.mjs` искал только двойное перекодирование (`Р°`, `СЃ`) и пропускал
символы-замены. В `src/data/site.ts` нашлись три `U+FFFD`: заголовок «КИТ-Строй.Москва»
и два текста кейса. Причина — сохранение файла через PowerShell в предыдущих задачах.
Текст восстановлен, а `check-mojibake.mjs` теперь ищет и `U+FFFD`, и возвращает ненулевой
код, если что-то найдено. Расширения по умолчанию включают `css`.

### 5. Карта в теме «КИТ-Строй.Москва»
Если `openstreetmap.org` не отвечает, на странице контактов оставалась пустая белая область
высотой 560 px: у iframe не было подстраховки. Добавлена подпись со ссылкой на маршрут;
SCSS пересобран (`npx sass assets/src/scss/style.scss:assets/dist/css/main.css` — команда
записана в `README.md` темы). Тема правится локально, в git не попадает: клиентский проект.

## Новые проверки
| Скрипт | Что делает |
| --- | --- |
| `scripts/audit-visual.mjs` | прокрутка как у человека, появление блоков, картинки, провалы, переполнение; `--shots`, `--base`, `--paths`, `--widths`, `--min-gap` |
| `scripts/live-audit.mjs` | тот же разбор для живого адреса |
| `scripts/case-shots.mjs` | пересъёмка скриншотов кейсов с проверкой кадра на пустые полосы |
| `scripts/audit-image.mjs` | разбор готового снимка: однотонные полосы и доля пустой высоты |
| `scripts/lib/audit.mjs`, `scripts/lib/image-bands.mjs` | общие выражения и разбор изображений |
| `scripts/slice-image.mjs` | нарезка длинного снимка на части для визуального просмотра |

`npm run check:visual`, `npm run check:images`, `npm run check:mojibake` — те же проверки
через package.json. В workflow «Проверки» добавлен шаг визуальной проверки, отчёт и скриншоты
остаются артефактом.

## Что проверено
- `npm run build` — типы и сборка без ошибок, три страницы сборки.
- `node scripts/check-responsive.mjs`: на 360 / 768 / 1440 нет горизонтального переполнения
  и элементов за краем; 4/4 скриншота загружаются; якоря целые; cookie-уведомление показывается,
  принимается и запоминает выбор; мобильное меню, три шага квиза, страница политики.
- `node scripts/audit-visual.mjs` — все страницы и ширины: пустых или непоявившихся блоков нет,
  битых и недогруженных картинок нет, переполнения нет, провалов больше 280 px нет.
- `node scripts/audit-image.mjs` по всем сохранённым снимкам: пустой высоты 0–5,6 %.
- Живые сайты: `masterskaya-forma.ru` (главная и конфигуратор) и `rootlost.ru/contacts/` —
  разобраны тем же способом, непоявившихся блоков нет; пустые полосы в кадре `forma-full.webp`
  были дефектом снимка, а не сайта.
- Текст на трёх страницах портфолио (главная, кейс, политика): 684 строки, испорченных символов нет.

## Что осталось демонстрационным
- Демо-сайт мастерской: реквизиты, адрес, телефон и цены вымышлены, пометка есть в подвале,
  на странице контактов и в `src/data/site.mjs` (`site.demo`).
- Иллюстрации в кейсах — чертёжные заглушки, нарисованные кодом; настоящих фотографий объектов нет.
- Кейс Sertexity — вёрстка по макету, сайт без живого адреса.

## Что нужно от владельца
- Сменить пароль FTP (передавался в переписке).
- Сказать, если карта OpenStreetMap на `rootlost.ru/contacts/` раньше отображалась: тогда
  пустая область была разовой, а подпись со ссылкой осталась подстраховкой.
- Выгрузить обновлённую тему `kitstroy-moscow` на `rootlost.ru`: правки темы лежат только
  локально, в git клиентские проекты не попадают.

## Следующий шаг
- Живой `rootlost.online` пока отдаёт прошлую сборку (`main-CPW7DZZJ.js`), локальная —
  свежая (`main-DiNPJOWn.js`): нужна публикация портфолио (workflow «Публикация»).
- Если у демо-сайта появится реальный заказчик — заменить реквизиты в `src/data/site.mjs` и снять флаг `demo`.
- Заменить чертёжные заглушки изображений на фотографии объектов, когда они появятся.
- Если у Александры появится отдельный бренд или логотип — заменить монограмму «АМ» в `src/components/Logo.tsx`.
